# Rescue

This directory is reserved for the custom Reforge rescue environment.

- `overlay/` contains files that will be layered into the rescue filesystem.
- `build/` contains notes and helper scripts related to rescue-image assembly.
